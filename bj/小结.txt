1.最小的存储单位是位，可以存储0和1；
2.字节是常用的计算机存储单位，对于几乎所有的机器，1字节为8位。
3.字是设计计算机时给定的自然存储单位，计算机的字长越大，其数据转移的越快，允许的内存访问越大。

4.在使用printf打印时，假若为float类型的数据，%0.2f表示打印小数点后两位。
5.return 0的作用，在写c语言的时候，我们的主函数基本是int main（）,括号代表它是一个函数，括号里面的数字代表参数，括号前面是int 类型的函数，说明最后函数的返回值应该是int类型，return 0就是结束该函数。